<div class="d-flex justify-content-between align-items-center mb-3">
    <div class="d-flex align-items-center">
        <label for="tanggalbahan" class="col-form-label me-2">Tanggal</label>
        <input type="month" class="form-control" id="tanggalbahan" name="date" value="{{ $dateParam }}">
    </div>
</div>

<h5 class="fw-bold mt-4 text-primary">Ringkasan Terbuang - BAR</h5>
<table class="table table-bordered table-sm">
    <thead class="table-light">
        <tr>
            <th>No.</th>
            <th>Nama Bahan</th>
            <th class="text-end">Terbuang (Total)</th>
        </tr>
    </thead>
    <tbody>
        @forelse ($totalTerbuangBar as $index => $item)
            <tr>
                <td>{{ $index + 1 }}</td>
                <td>{{ $item['name'] }}</td>
                <td class="text-end">{{ number_format($item['total_terbuang'], 3, ',', '.') }}</td>
            </tr>
        @empty
            <tr>
                <td colspan="3" class="text-center">Tidak ada data</td>
            </tr>
        @endforelse
    </tbody>
</table>

<h5 class="fw-bold mt-4 text-primary">Ringkasan Terbuang - KITCHEN</h5>
<table class="table table-bordered table-sm">
    <thead class="table-light">
        <tr>
            <th>No.</th>
            <th>Nama Bahan</th>
            <th class="text-end">Terbuang (Total)</th>
        </tr>
    </thead>
    <tbody>
        @forelse ($totalTerbuangKitchen as $index => $item)
            <tr>
                <td>{{ $index + 1 }}</td>
                <td>{{ $item['name'] }}</td>
                <td class="text-end">{{ number_format($item['total_terbuang'], 3, ',', '.') }}</td>
            </tr>
        @empty
            <tr>
                <td colspan="3" class="text-center">Tidak ada data</td>
            </tr>
        @endforelse
    </tbody>
</table>

